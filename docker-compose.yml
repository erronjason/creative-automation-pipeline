services:
  app:
    build: .
    ports: ["8765:8765"]
    env_file:
      - path: .env
        required: false
    volumes:
      - ./briefs:/app/briefs
      - ./assets:/app/assets
      - ./output:/app/output
      - ./.cache:/app/.cache
