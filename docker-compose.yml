services:
  app:
    build: .
    ports: ["127.0.0.1:8765:8765"]  # loopback only: the UI is unauthenticated
    env_file:
      - path: .env
        required: false
    volumes:
      - ./briefs:/app/briefs
      - ./assets:/app/assets
      - ./output:/app/output
      - ./.cache:/app/.cache
